node_modules/
# Копии сайта — собираются скриптом build-www.ps1, в истории git не нужны
www/
android/app/src/main/assets/public/
android/app/build/
android/.gradle/
android/build/
android/local.properties
store-assets/*.zip
store-assets/googleplay-package/signing.keystore
store-assets/googleplay-package/signing-key-info.txt
# Собранные пакеты — по 2 МБ на каждую пересборку, в истории git не нужны
store-assets/googleplay-package/*.aab
store-assets/googleplay-package/*.apk
# Ключ в виде текста для секрета GitHub — тоже секрет, в историю не кладём
store-assets/googleplay-package/*.base64.txt
