<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Тема самого приложения. Её ставит MainActivity сразу после запуска.
         Синий фон окна виден в полосах под строкой состояния и панелью
         навигации — отступы под них проставляет MainActivity. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/hcSystemBars</item>
    </style>

    <!-- Заставка. По умолчанию Capacitor показывает здесь СВОЙ логотип —
         для проверяющего в магазине это признак стандартной обёртки вокруг
         сайта. Картинку перерисовывает generate-splash.ps1 нашим знаком,
         а Android 12 и новее рисует заставку сам по трём строкам ниже. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/hcSplashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
