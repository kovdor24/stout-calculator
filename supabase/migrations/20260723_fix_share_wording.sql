-- ============================================================================
--  ФОРМУЛИРОВКА: «расшаренная ссылка» → «ссылка, отправленная клиенту»
-- ============================================================================
-- Правим description в каталоге значков (сейчас нигде на фронте не читается,
-- только для порядка в самой базе — фронт использует свой текст в BADGES).
--
-- Выполнить вручную в Supabase SQL Editor.
-- ============================================================================

update public.achievements
   set description = 'Первая ссылка, отправленная клиенту'
 where id = 'first_contact';

update public.achievements
   set description = '20 ссылок, отправленных клиентам'
 where id = 'sales_master';
