name: Weekly Price Parser

on:
  schedule:
    - cron: '0 2 10 * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium beautifulsoup4
      - name: Run parser script
        run: python AutoPrice.py
      - name: Commit and push changes
        # always() — коммитим и пушим то, что парсер успел собрать, даже если сам скрипт
        # упал с ошибкой (например, блокировка CDN на середине списка); при этом джоба
        # в целом всё равно останется помечена как failed из-за упавшего предыдущего шага.
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add catalog.js
          if git diff --staged --quiet; then
            echo "Цены и статусы не изменились."
          else
            git commit -m "Автообновление цен"
            git push
          fi
