name: Company Info (ГИР БО)

# Официальные данные по ИНН дистрибьюторов: выручка, прибыль, активы, статус,
# уплаченные налоги и численность работников. Источников два, оба открытые и
# без ключей: ГИР БО (bo.nalog.gov.ru) — бухгалтерская отчётность, открытые
# данные ФНС (nalog.gov.ru/opendata) — налоги и численность.
# Список ИНН скрипт берёт из своей базы (RPC analytics_distributor_inns) —
# заводит их владелец в карточке дистрибьютора.
#
# Второй источник отдаётся только архивом на всю страну — это 320 МБ загрузки
# и минута работы на прогон. Если ФНС не отдаст архив (в том числе если
# закроет доступ зарубежным адресам, с которых ходит runner), скрипт напишет
# «не собрано» и продолжит: отчётность из ГИР БО от этого не пострадает.
#
# Раз в месяц, 9-го числа. Чаще бессмысленно: годовая отчётность обновляется
# один раз, до конца марта следующего года. Ежемесячный прогон нужен не ради
# выручки, а ради статуса компании и новых дистрибьюторов, которых завели
# после прошлого запуска.
on:
  schedule:
    - cron: '0 6 9 * *'
  workflow_dispatch:
    inputs:
      inn:
        description: 'Один ИНН (для проверки). Пусто — все дистрибьюторы из базы'
        required: false
        default: ''

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      # Зависимостей нет: скрипт ходит в ГИР БО, в открытые данные ФНС и в
      # Supabase через urllib, архивы читает штатными zipfile и ElementTree.
      - name: Collect company info
        run: |
          if [ -n "${{ github.event.inputs.inn }}" ]; then
            python AutoCompanyInfo.py --inn "${{ github.event.inputs.inn }}"
          else
            python AutoCompanyInfo.py
          fi
      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add analytics/companies.json
          if git diff --staged --quiet; then
            echo "Данные по компаниям не изменились."
          else
            git commit -m "Автообновление данных по ИНН дистрибьюторов (ГИР БО)"
            for i in {1..5}; do
              git pull --rebase origin main && git push origin main && exit 0
              git rebase --abort || true
              sleep 5
            done
            exit 1
          fi
